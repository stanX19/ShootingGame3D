2. Trail tag for rendering, will draw a line trail for objects previous positions
	- add struct to record previous positions
3. tags::faction::{
		ally,
		enemy,
		other1,
		other2,
	}
	enemies = registry.view<tag::Targetable>(entt::exclude<MyTag>)

	// abusing std::tuple, std::apply, and fold expression and decltype to iterate through types
	using AllFactionTags = std::tuple<Ally, Enemy, Neutral, Other1, Other2>;
	AllFactionTags allFactionTags;

	std::apply([&](auto... tag_type) { ( {
		enemies registry.view<decltype(tag_type)>(entt::exclude<decltype(tag_type)>)
	}, ...) }, allFactionTags)

4. replace AimTarget with AimTarget {
	entity
	locktime
	lockedOn
}; if in cone for locktime > radar.locktime then lock

5. struct Radar {
	detectDistance
	lockAngle
	lockDistance
	lockTime
}
7. enemy shoot control if locked: weapon shoot
8. aim assist only start when locked, use previous positions instead of straight prediction
10. make constants be read from .conf files instead of hardcoded
11. Collision
	- seperate collision detection and actual processing
	- seperate death logic of different entities using tags
12. tag::SpaceShip + SpaceshipControl {float foward; quat turnDir} + tag::PlayerControl / tag::AIControl
13. model_manager apply scaling to model when loading such that new scale reflects real scale (compared to unit sphere)
15. Realistic physics, no more breaking in vacum
16. Have another system to assign bitwise faction value
17. number control for weapon firing
18. make camera a component for playable entities
	struct Camera {
		Camera3d camera;
		firstPerson = false;
		alignHorizontal = false;
	}