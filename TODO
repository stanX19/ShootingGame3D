3. tags::faction::{
		ally,
		enemy,
		other1,
		other2,
	}
	enemies = registry.view<tag::Targetable>(entt::exclude<MyTag>)

	// abusing std::tuple, std::apply, and fold expression and decltype to iterate through types
	using AllFactionTags = std::tuple<Ally, Enemy, Neutral, Other1, Other2>;
	AllFactionTags allFactionTags;

	std::apply([&](auto... tag_type) { ( {
		enemies registry.view<decltype(tag_type)>(entt::exclude<decltype(tag_type)>)
	}, ...) }, allFactionTags)

8. aim assist only start when locked, use previous positions instead of straight prediction
10. make constants be read from .conf files instead of hardcoded
12. tag::SpaceShip + SpaceshipControl {float foward; quat turnDir} + tag::PlayerControl / tag::AIControl
15. Realistic physics, no more breaking in vacum
16. Have another system to assign bitwise faction value
17. number control for weapon firing
18. make camera a component for playable entities
	struct Camera {
		Camera3d camera;
		firstPerson = false;
		alignHorizontal = false;
	}
19. enemy dodge asteroids
25. sound component, sound system
26. burning effect when hit, burning particle, burning system
28. ChargedWeapon {
	float chargeTime; // time to fully charge
	float cooldownTime; // time after firing to start charging again
	float currentCharge; // current charge level
	bool isCharging; // whether the weapon is currently charging
}
29 fix missile by using aimtarget component and ai movement system
	- add weapon componenet with negative bullet velocity so it kills itself
	- missile should not affect aim circle
	- missile should not change aim target & only have small vision cone
30. mass system for physics calculation & death velocity inheritance
31. cone Trail struct {
	float radius;
	float lifespan;
	Vector3 color;
	Vector3 prevPos1;
	Vector3 prevPos2;
	Vector3 prevPos3;
}
32. last enemy hit (record 2 entity uppermost parent and their damage) component for score calculation
33. killing ally no score